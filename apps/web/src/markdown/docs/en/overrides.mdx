# Overrides

Trans allows you to override translations for specific keys. This is useful when you want to ensure that a specific translation is used for a particular key, regardless of the default translation.

## How it works

In the dashboard, you can create overrides for specific keys. You can also create overrides for specific languages.